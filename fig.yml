samantha:
  build: .
  command: python run.py
  ports:
   - "5000:5000"
  volumes:
   - .:/code
  links:
   - redis
  environment:
    FLASK_HOST: 0.0.0.0
    FLASK_IS_DEBUG: true
    REDIS_HOST: redis_1
    REDIS_PORT: 6379

redis:
  image: redis
